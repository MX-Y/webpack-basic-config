import React from 'react';
import './header.scss';

const Header = () => (
	<header className="container-lg header">
		<i className="ali-icon icon-user" />
	</header>
);

export default Header;
